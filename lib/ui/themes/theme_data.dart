import 'package:flutter/material.dart';

class ThemeData {
  // This class houses theme data to be used across the app.
  // You can add different themes and switch between them as required.
  // Example theme:
  // static final ThemeData defaultTheme = ThemeData(
  //    primaryColor: Colors.blue,
  //    barColor: Colors.white,
  //    fontFamily: 'Sanserif',
  //    buttonTheme: ButtonThemeData(textTheme: TextTheme(...defaultTextTheme)),
  // );
}